interface IProps {
  amount: number;
  usdValue: number;
  formattedAmount: string;
}

export default function WalletRow({
  amount,
  usdValue,
  formattedAmount,
}: IProps) {
  return <div></div>;
}
